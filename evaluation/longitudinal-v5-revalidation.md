# DoctorWhy v5.0.0 纵向评测复核记录

## 复核范围

本记录只更新当前源码分支的复核状态，不覆盖 2026-08-12 原始评测材料，也不把 PathoFly HTML 演示结果当作 DoctorWhy v5.0.0 的当前运行结果。

## 复核结论

| 编号 | 当前状态 | 处理决定 | 证据边界 |
|---|---|---|---|
| F001 | 未完成当前版本独立复测 | 暂不修改核心规则 | 当前 v5 的 Phase 1 明确要求先复述并等待用户确认；旧证据来自历史演示场景，不能证明当前运行仍复现。 |
| F002 | 静态规则已存在，运行表现未独立复测 | 暂不新增进度条或大面积状态 UI | 当前 v5 已写明 Phase 1–5 及转换条件；是否每轮实际展示仍需同环境复测。 |
| F003 | 静态规则已存在，长对话表现未独立复测 | 暂不修改决策登记表规则 | 当前 v5 要求每轮维护决策登记表；旧证据不能替代当前版本长对话测试。 |
| F009 | 当前版本未独立复测 | 暂不修改 Phase 5 核心规则 | 当前 v5 已明确“未打满勾禁止提交”；旧 F009 证据来自 PathoFly 演示输出，不是当前源码运行链。 |
| F007 | 当前版本无完整运行闭合证据 | 暂不声称已修复 | 旧记录只证明当时轨迹缺少终态，不能归因于当前插件。 |
| F011 | Python 3.8 兼容性未被项目声明 | 不扩大兼容承诺 | 当前仓库未声明最低 Python 版本，因此不把一次旧环境失败直接改成兼容性缺陷。 |
| F012 | 当前用例明确标记“自评、待独立复核” | 暂不改写历史评分 | 当前 `evaluation/cases.md` 没有把待复核结果写成正式独立通过。 |
| F013 | 当前脚本可静态确认 | 已修复默认路径和 C1–C6 分块 | 原脚本使用相对 `cases.md` 路径，并按旧标题切块；当前脚本改为脚本目录解析并识别当前用例标题。 |

## 本次实际修改

- 只修改 `evaluation/eval_score.py` 的默认输入路径和用例分块逻辑。
- 新增本复核记录，不修改原始评测目录。
- 新增 `evaluation/test_eval_score.py`，覆盖当前标题、旧标题、任意工作目录和空输入。
- 没有修改 `SKILL.md`、`AGENTS.md` 或任何 PathoFly HTML 文件。

## 尚未完成

- 未完成当前 v5 的真实多轮对话复测；
- 未完成独立评分人盲评；
- 未确认 F007 的当前运行闭合行为；
- 未创建提交或推送到 GitHub。
