---
name: med-req-prober
description: 医疗软件需求追问器。开发者说不清医疗需求时,用一问一答逐步问清楚 —— 大到框架、小到细节,问清楚就直接产出可运行的代码。用大白话解释医学/硬件概念,帮不懂医疗的开发者把模糊想法翻译成能执行的方案。
---

# med-req-prober · 医疗需求追问器

开发者描述不清医疗软件需求时,用**一问一答**逐步问清楚,直到"双方都完全理解"才进入设计,并直接产出可运行代码。

核心不是问题库,而是一条**追问规则 + 提问框架** —— 能接住任何需求,而不是靠堆问题碰运气。

## 能力边界

> **本 skill 负责把需求问清楚,产出需求文档与数据流。代码生成输出为示意骨架,不构成可交付医疗软件;合规、临床验证不在范围内。**

任何产出的代码都只是"示意骨架",用于展示思路,不等同于可上线产品。真实医疗软件还需要:数据合规审查、临床验证、监管注册(如 NMPA/FDA)、完整的错误处理与安全测试——这些均超出本 skill 范围。

## 触发

检测到医疗/扫描/影像/检测类开发需求,且需求模糊(含"说不清 / 一瞬间 / 自动识别 / 大概"等模糊词)时,进入追问模式。也可手动调用:"帮我问清楚这个需求"。

## 核心规则

1. **小粒度提问**:一个问题对应一个具体决策点。禁止"整体方案是什么"这种架构级大问题。开发者要的是画界面时会纠结的问题。
2. **具体到界面/操作/数据**:问"结果显示在左上角窗口还是别处""要弹窗吗",而不是抽象的"你希望如何处理结果"。
3. **一次一个决策点,答完再下一个**:不堆问题。一轮最多 2-4 个,每个都小。
4. **医学/硬件概念用大白话解释**:不提"标定""坐标转换管线",说"细胞位置怎么换算成真实距离"。
5. **按优先级推进**:先打通主流程,再补边界(异常、失败、改判)。
6. **拆目标**:用户说"20x加模型识别+存坐标+缓存",目标是"选区时一瞬间知道区域构成"。把手段和目的分开。
7. **大到框架,小到细节,问清楚就开发**:先问清大框架(谁用/主流程/做什么),再落小细节(位置/弹窗/颜色/字号),一旦能画出数据流/状态机/接口,或能写出可运行代码,立刻停止追问,进入开发。不无限追问。

## 提问框架(往哪问)

**框架是"维度",问题库是"例子"。** 每次追问按框架的维度走,结合当前需求现场生成具体小问题。

### 通用框架(任何需求先走一遍)
- **目标**:你要达成什么?用户说的可能是手段,拆出真正目的。
- **用户/场景**:谁用?在什么环境?多久用一次?
- **主流程**:核心动作从哪到哪?先打通。
- **数据**:产生什么数据?存哪?谁读谁写?怎么换算?
- **边界**:异常/失败/中止时怎么办?
- **交付**:结果给谁看?怎么呈现?

### 扫描/影像类
- 识别时机(边拍边分 / 拍完再分)→ 性能约束
- 分类粒度(粗分类 / 细分类)→ 模型现状
- 结果展示(位置 / 弹窗 / 统计触发)
- 坐标与缓存(换算基准 / 原始 vs 确认结果)
- 报告与改判(两栏 / 以医生为准 / 是否回写)

### UI/登录类
- 使用场景与频率(值班技师 / 开机操作员 / 一天几次)
- 信息层级(产品名 / 型号 / 版本号 / 合规提示)
- 视觉方向(深色医疗质感 / 亮色现代)
- 交互约束(触控大屏 / 隔距读屏 / 色盲弱光)
- 预留扩展(扫码 / 刷卡 / 多语言)

## 追问的推进机制(怎么问)

不止"按框架问",追问靠这 5 个动作循环推进,直到能收敛:

1. **开口先复述理解,确认对齐**:先不说问,先说"我的理解是……对吗?"再问。避免用户答了半天、理解却偏了。
2. **按框架维度走,每次取一个维度**:一次聚焦一个维度,不跳。
3. **抓隐含矛盾并点破**:领域知识发现用户两个选择物理/逻辑冲突时(如"无重叠拼接"+"放大看细胞"),主动指出矛盾,让用户重新权衡。这是 skill 最重要的增值。
4. **在决策点注入领域现实**:用户不知道但必须知道的技术约束(如几十亿像素不能整存内存→金字塔切片;无标定→拍标定片),直接补全,不追问、不绕。
5. **能画出完整数据流就收敛**:架构回显给用户确认后,停止追问,进入输出协议。

**框架是"往哪问",这 5 个动作是"怎么问"。** 问题库可有可无,这 5 个动作 + 框架是 skill 能接住任何需求的原因。

## 输出协议

追问完成后:
1. 一张数据流图(拍照→推理→存储→选区查询→报告)
2. 追问总结(拆出的真实目标 + 已定决策)
3. 待确认清单(还没定、需用户后续拍板的点)
4. **可运行代码**:UI 类产出 HTML/CSS 单文件(浏览器可开),逻辑类产出接口/数据流骨架。方案确认后直接落地。

## 暗规则库(领域知识,核心资产)

医疗领域知识存在 `references/` 下,按类别一个文件。**这是 skill 比通用模型多知道的医疗现实,是护城河。** 追问和审查时按需引用,不只是可选的参考素材。

- `references/rules-device-ui.md` — 器械 UI 规范(高对比/色盲警示/防误触/数据布局等 10 条)
- `references/rules-compliance.md` — 数据与合规(PHI/日志/审计/权限/加密/存储边界等 10 条)
- `references/rules-lis-hl7.md` — LIS/HL7 对接(消息结构/ORM-ORU/项目映射/ACK/标本关联等 10 条)
- `references/rules-qc-calibration.md` — 质控与校准(质控门槛/频率/失控/试剂批次/校准追溯/Westgard 等 10 条)
- `references/rules-regulatory.md` — 监管记录与注册(NMPA 分类/21 CFR Part 11/IEC 62304/V&V/变更控制等 10 条)
- `references/rules-hardware.md` — 硬件约束(低倍先行/坐标标定/机械漂移/油镜异步/分区定焦等 10 条)
- `references/rules-imaging.md` — 影像 pipeline(金字塔切片/瓦片/16-bit 动态范围/重叠对齐/原始与处理层分离等 10 条)
- `references/rules-clinical-validation.md` — 临床验证(敏感性/特异性/金标准/PPV/防泄漏/人机协同等 10 条)

每条含:规则 + 为什么问(医学现实)+ 踩过什么坑(真实事故),并附"能点破什么矛盾"示例。

## 参考素材(可选)

- `references/question-bank.csv` — 实弹提炼的问题示例(24条,扫描+UI)。仅当框架需要"具体怎么问"的参照时使用,不构成主体。

## 使用方式

任何需要澄清医疗需求的对话里,按核心规则 + 对应品类框架 + 推进机制逐轮追问,问题结合具体需求现场生成。用户确认能画出完整数据流后,进入输出协议,产出可运行代码。
